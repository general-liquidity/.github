# Normalize line endings to LF in the repo so the workflow's shell step and the
# Node generator behave the same regardless of contributor OS.
* text=auto eol=lf
*.md text eol=lf
*.mjs text eol=lf
*.yml text eol=lf
*.svg text eol=lf
